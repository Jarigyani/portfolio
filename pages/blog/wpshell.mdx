import MLayout from '@/MLayout'

![eyecatch](https://drive.google.com/uc?id=1LdPeq411NEqXS3MVJANb8rDQda4aQJ6y)

# Create server 🍭
まずはいつも通りcentos7.9でサーバーを作成してください

入力する場所と内容は以下の通りです

|アーカイブ|管理ユーザーのパスワード|ホスト名|名前|タグ|
| - | - | - | - | - |
|CentOS 7.9(2009) 64bit|自由|学籍番号|学籍番号|学籍番号|

上記の入力が終わったら、サーバー作成をしてください

この時、時価が9円か12円になっていることを確認してから作成してください

# curl🐶
コンソールにログインして下記を入力

```
curl https://raw.githubusercontent.com/Jarigyani/weather-app/main/front/src/assets/dcwp.sh > ~/dcwp.sh
```

# 実行
👇g000c0000を自分の学籍番号に書き換えて入力

> 実行すると色々出ますが放置して次の手順に進んでください
> 学籍番号等を間違えたまま実行してしまった場合はcontrol+cで一度停止して、再度修正したコマンドを実行してください
```
bash ~/dcwp.sh g000c0000
```

# DNS setting ✈️
先ほどのコマンドは完了まで時間がかかるのでその間にDNSレコードの設定をします

👇画面から自分のサーバーのIPアドレスをDNSのレコードに設定してください

![](https://drive.google.com/uc?id=1pI0IIJDAtkcn3YGdbWtEYPoqyzGvJIOb)

# 起動確認😎
ブラウザで `[学籍番号].neec-ss14.click` にアクセスして👇みたいになってたら完了

![](https://drive.google.com/uc?id=1m1hsiSrtV4uvrIXcIPOmhfAcFdrN5x3j)

できない場合は声かけてください🙏

export default ({children}) => <MLayout text="docker+wordpress(CentOS7.9)">{children}</MLayout>